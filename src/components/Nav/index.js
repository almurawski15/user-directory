import React from "react";

function NavBar () {
    return (
        <nav>
            <div>
                <h1> Employee Directory </h1>
            </div>
        </nav>
    );
}

export default NavBar;